# 시간복잡도와 Big-O 계산

`my_math.c`에 구현된 `get_gcd` 함수는 유클리드 호제법을 사용한다.
나머지 연산(`a % b`)이 진행될 때마다 입력값의 크기가 지수적으로 감소하며,
수학적으로 두 회차마다 값이 최소 절반 이하로 줄어듦이 증명되어 있다.
따라서 입력값 $N$에 대해 연산 횟수는 이진 로그에 비례하며,
최종 시간 복잡도는 `O(log N)`이다
